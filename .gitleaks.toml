title = "Servio.AI gitleaks configuration"

# Keep allowlists as narrow as possible. Do NOT allowlist generic secret regexes.
# Only exclude specific documentation files that intentionally include example strings.

[allowlist]
description = "Limit allowlist to specific docs only (no generic regexes)"
paths = [
  '''doc/COMO_CONFIGURAR_CHAVES\.md'''
]
