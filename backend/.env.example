# Gmail Service Configuration
# 
# To send emails via Gmail SMTP:
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
#    - Select App: Mail
#    - Select Device: Other (Custom name) → "Servio.AI Backend"
#    - Copy the 16-character password
# 3. Replace values below with your credentials

# Gmail account to send emails from
GMAIL_USER=your-email@gmail.com

# App Password (16 characters, no spaces)
# NOT your regular Gmail password!
GMAIL_APP_PASSWORD=abcd efgh ijkl mnop

# Optional: Use Gmail API instead of SMTP (requires more setup)
# USE_GMAIL_API=false

# Rate limiting (API)
# Window in ms and max requests per window for critical endpoints (/login, /register, /users POST, /proposals POST)
# API_RATE_LIMIT_WINDOW_MS=60000
# API_RATE_LIMIT_MAX=20
