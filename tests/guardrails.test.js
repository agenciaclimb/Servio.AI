// Arquivo legado desativado.
// Os testes ativos deste módulo agora vivem em: guardrails.test.ts
//
// IMPORTANTE: Vitest falha quando um arquivo de teste não contém nenhuma suite.
// Mantemos um placeholder skip para evitar regressão no gate `npm test`.

import { describe, it } from 'vitest';

describe('guardrails.test.js (legado)', () => {
	it.skip('desativado (migrado para guardrails.test.ts)', () => {});
});

export {};
