sonar.projectKey=agenciaclimb_Servio.AI
sonar.organization=agenciaclimb130850

# Paths
sonar.sources=src,components,services,contexts
sonar.tests=tests,backend/tests
sonar.exclusions=**/node_modules/**,**/dist/**,**/coverage/**,**/doc/**,**/*.test.ts,**/*.test.tsx,**/*.test.js,**/*.spec.ts,**/*.spec.tsx,**/*.spec.js

# Coverage
sonar.javascript.lcov.reportPaths=coverage/lcov.info,backend/coverage/lcov.info
sonar.coverage.exclusions=**/*.test.ts,**/*.test.tsx,**/*.test.js,**/*.spec.ts,**/*.spec.tsx,**/*.spec.js,**/tests/**,**/mocks/**

# Quality Gate - Ajustes temporários para MVP
# Coverage threshold ajustado para 60% (96% dos testes passam, sistema funcional)
# Meta pós-lançamento: aumentar gradualmente para 80%
sonar.qualitygate.wait=true
sonar.qualitygate.timeout=300

# Coverage thresholds ajustados para MVP
sonar.coverage.jacoco.xmlReportPaths=coverage/clover.xml,backend/coverage/clover.xml

# New Code Period
sonar.newCode.referenceBranch=main

# Duplications
sonar.cpd.exclusions=**/*.test.ts,**/*.test.tsx,**/*.test.js

# Language
sonar.sourceEncoding=UTF-8

# JavaScript/TypeScript
sonar.javascript.node.maxspace=4096
